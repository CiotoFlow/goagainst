* Union / Sum types

http://www.jerf.org/iri/post/2917

* Entitità

type Entity interface {
	Name() string
	isEntity()
}

Non si lavora per nickname, o nomi di channel, ma per entità.
In questo modo, se un utente cambia nick, viene cambiato in un solo posto.


